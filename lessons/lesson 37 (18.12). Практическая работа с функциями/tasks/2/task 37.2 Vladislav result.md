result: 40/100

### 1. Формулировка задания
Требуется создать функцию `delivery_price(city, weight_kg, urgent)`, которая:
- При `urgent=True` возвращает стоимость доставки по формуле `500 + 30 * weight_kg`.
- При `urgent=False` (или отсутствии срочности) использует формулу `300 + 20 * weight_kg`.
- Параметр `city` пока не влияет на расчёты, но добавлен для читаемости и будущих расширений.
- Необходимо вызвать функцию 4 раза: дважды позиционно и дважды с именованными аргументами.
- В комментарии добавить намеренно некорректный вызов, приводящий к `TypeError` или `SyntaxError`.

### 2. Результаты проверки
**Файл:** `lessons/lesson 37 (18.12). Практическая работа с функциями/tasks/2/vlad.py`  
**Проверяемые элементы:**
1. **Название функции** (строка 1):  
   Ожидалось: `delivery_price`.  
   Реализовано: `delivery_cost` → **ошибка имени функции**.
2. **Количество вызовов** (строка 6):  
   Ожидалось: 4 вызова (2 позиционных, 2 именованных).  
   Реализовано: 1 вызов (именованный) → **недостаёт 3 вызовов**.
3. **Некорректный вызов в комментарии** (отсутствует):  
   Требовалось добавить закомментированный пример с ошибкой → **не реализовано**.

**Шаги воспроизведения:**
- Запуск кода проходит без ошибок, но выводится только один результат.
- Логика расчёта стоимости корректна, но требования к сигнатуре функции и вызовам не выполнены.

### 3. Сильные стороны работы
- **Корректность формул**: Расчёты для срочной/несрочной доставки реализованы верно.
- **Читаемость кода**: Имена параметров понятны, код отформатирован согласно PEP 8.

### 4. Ошибки и рекомендации
**Блокирующие ошибки:**
1. **Неверное имя функции** (строка 1):  
   Функция названа `delivery_cost` вместо `delivery_price`.  
   **Исправление**:  
   ```python
   def delivery_price(city, weight_kg, urgent):
   ```

**Значимые ошибки:**
1. **Недостающие вызовы функции** (отсутствуют):  
   Требуется добавить:
   - 2 позиционных вызова (например, `delivery_price("Москва", 5, False)`).
   - 1 именованный вызов (например, `delivery_price(city="Сочи", weight_kg=1.5, urgent=True)`).
2. **Отсутствие некорректного вызова в комментарии**:  
   **Исправление**: Добавить в комментарий пример с ошибкой, например:  
   ```python
   # delivery_price("Омск", weight_kg=3, True)  # Вызовет TypeError: positional argument follows keyword argument
   ```

### 5. Оценка
- **Функциональность (50%)**: 10/50  
  Снято за: неверное имя функции (-20%), отсутствие 3 вызовов (-15%), отсутствие примера ошибки (-5%).
- **Качество кода (30%)**: 20/30  
  Снято за: несоответствие сигнатуры функции (-10%).
- **Стиль и тесты (20%)**: 20/20  
  Код читаем, нарушений стиля нет.
- **Итог**: 40/100.

### 6. Вариант полного решения
```python
def delivery_price(city, weight_kg, urgent):
    if urgent:
        return 500 + 30 * weight_kg
    return 300 + 20 * weight_kg

# Позиционные вызовы
print(delivery_price("Москва", 5, False))  # 300 + 20*5 = 400
print(delivery_price("Екатеринбург", 1.2, True))  # 500 + 30*1.2 = 536

# Именованные вызовы
print(delivery_price(city="Казань", weight_kg=2, urgent=True))  # 500 + 30*2 = 560
print(delivery_price(weight_kg=3, city="Сочи", urgent=False))  # 300 + 20*3 = 360

# Пример намеренно неправильного вызова (раскомментировать для проверки ошибки)
# delivery_price("Омск", weight_kg=3, True)  # TypeError: positional argument follows keyword argument
```

---

**Анализ выполнен моделью:** GPT-4o
