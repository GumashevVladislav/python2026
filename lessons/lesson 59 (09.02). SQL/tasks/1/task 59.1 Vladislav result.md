Время затраченное на выполнение: 0:34

result: 20/100

1) **Сильные стороны**
- Решение содержит корректный синтаксис SQL для первого запроса (извлечение всех данных).
- Код лаконичен, без излишней сложности.

2) **Ошибки и недочёты**

**Блокирующие (ломает выполнение требований задания)**
- Задание требует три отдельных запроса для Уровня 1. Предоставлено только два SQL-оператора. Отсутствует третий запрос: "Получить список имен всех женщин, которые путешествовали первым классом". Это невыполнение основной задачи.
- Второй предоставленный запрос `SELECT Name, Age sex FROM titanic;` не соответствует условию задачи Уровня 1, пункт 2: "Извлечь информацию об именах пассажиров старше 30 лет". В запросе отсутствует условие `WHERE Age > 30`. Также присутствует синтаксическая ошибка: перечисление столбцов `Name, Age sex` (вероятно, опечатка, должно быть `Name, Age` или `Name, Age, Sex`). Даже без условия `WHERE` запрос не соответствует требованию.
- Полностью отсутствуют запросы для Уровня 2 (3 задачи) и Уровня 3 (3 задачи). Это критическое несоответствие условию задания.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- Второй запрос содержит потенциальную синтаксическую ошибку (`Age sex`), которая может привести к ошибке выполнения или неверному набору столбцов в результате.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- В файле решения отсутствуют комментарии, поясняющие, какой запрос какую задачу решает.
- Имена столбцов в условии приведены на русском ("имя", "возраст", "пол"), но в реальной таблице они, скорее всего, на английском (например, `Name`, `Age`, `Sex`). Студент использовал `Name` и `Age`, что, вероятно, верно, но в контексте задания следовало бы уточнить соответствие. Однако, так как в условии приведен только псевдокод, это не является ошибкой студента.

3) **Оценка и как она посчитана**
- Функциональность и соответствие условию: 10/50. Из 9 требуемых запросов корректно выполнен только 1 (первый). Второй запрос не соответствует условию и содержит ошибку. Остальные 7 запросов отсутствуют. Вычет 40 баллов.
- Качество кода: 5/30. Присутствует синтаксическая ошибка в одном из двух запросов. Структура минимальна, но задание не выполнено. Вычет 25 баллов.
- Стиль и тесты: 5/20. Комментарии отсутствуют. Тесты не требовались и не предоставлены. Вычет 15 баллов.
Итог: 10 + 5 + 5 = 20 баллов.

4) **Если задание выполнено не полностью**
- Отсутствует полностью: запрос для задачи 1.3 Уровня 1, все три запроса Уровня 2, все три запроса Уровня 3.
- Выполнено частично: задача 1.1 выполнена, задача 1.2 не выполнена (запрос не соответствует условию).

**Вариант полного решения (основано на псевдокоде из условия и предположении, что имена столбцов соответствуют общепринятым для датасета Titanic: `Name`, `Age`, `Sex`, `Pclass`, `Survived`, `SibSp`, `Parch`, `Fare`):**

```sql
-- Уровень 1
-- 1. Извлечь все данные о пассажирах без дополнительных условий
SELECT * FROM titanic;

-- 2. Извлечь информацию об именах пассажиров старше 30 лет
SELECT Name FROM titanic WHERE Age > 30;

-- 3. Получить список имен всех женщин, которые путешествовали первым классом
SELECT Name FROM titanic WHERE Sex = 'female' AND Pclass = 1;

-- Уровень 2
-- 1. Выведите имена и возраст выживших пассажиров, используя сортировку по возрасту
SELECT Name, Age FROM titanic WHERE Survived = 1 ORDER BY Age;

-- 2. Выведите имена пассажиров, которые путешествовали одни (без братьев, сестёр, супруга или супруги, родителей или детей)
SELECT Name FROM titanic WHERE SibSp = 0 AND Parch = 0;

-- 3. Выведите имена и классы мест тех пассажиров, стоимость билетов которых превышала 100
SELECT Name, Pclass FROM titanic WHERE Fare > 100;

-- Уровень 3
-- 1. Выведите список пассажиров (имя, класс, возраст), которые путешествовали не первым классом и были старше 18 лет
SELECT Name, Pclass, Age FROM titanic WHERE Pclass != 1 AND Age > 18;

-- 2. Выведите список пассажиров, которые не выжили и у которых не было родственников на борту
SELECT * FROM titanic WHERE Survived = 0 AND SibSp = 0 AND Parch = 0;

-- 3. Выведите имена, стоимость билетов и класс мест тех пассажиров, которые заплатили за билет меньше 10, но путешествовали не третьим классом
SELECT Name, Fare, Pclass FROM titanic WHERE Fare < 10 AND Pclass != 3;
```
