result: 15/100

### 1. Формулировка задания
Требуется написать функцию `mean_abs_deviation`, принимающую произвольное количество числовых аргументов (*args). Функция должна:
- Вызвать `ValueError` с сообщением при вызове без аргументов
- Вычислить среднее арифметическое переданных значений
- Вернуть кортеж абсолютных отклонений каждого значения от среднего, округлённых до одной десятой
- Обработать распаковку списка при вызове
- Гарантировать возврат кортежа (не списка)

### 2. Результаты проверки
**Файл:** `vlad.py`  
**Проблемы:**
1. **Отсутствует тело функции** (строки 1-5):  
   В коде нет определения функции `mean_abs_deviation` или `mean_absdeviation`. Приведены только вызовы несуществующей функции и фрагмент кода с `if not scores`.
2. **Синтаксические ошибки** (строка 2):  
   Проверка `if not scores` находится вне функции и использует необъявленную переменную `scores`.
3. **Несоответствие имён функций** (строки 1, 4):  
   В первом вызове используется `mean_absdeviation` (без подчёркивания), во втором — `mean_abs_deviation`.
4. **Не реализована основная логика**:  
   Нет расчёта среднего, отклонений, округления и формирования кортежа.

**Шаги воспроизведения:**
- Любая попытка запуска приведёт к ошибкам `NameError` (отсутствие функции) и `IndentationError` (неправильные отступы).

### 3. Сильные стороны работы
- Предпринята попытка обработки пустого ввода через `raise ValueError`.
- Есть пример вызова с распаковкой списка (`*existing_scores`), что соответствует требованию.

### 4. Ошибки
**Блокирующие (4 шт.):**
1. **Не объявлена функция**  
   Исправление: Добавить определение функции:
   ```python
   def mean_abs_deviation(*scores):
   ```
2. **Проверка пустого ввода вне функции**  
   Исправление: Перенести проверку внутрь функции:
   ```python
   if len(scores) == 0:
       raise ValueError("Нужно передать хотя бы один балл")
   ```
3. **Отсутствует расчёт среднего и отклонений**  
   Исправление: Добавить логику:
   ```python
   average = sum(scores) / len(scores)
   deviations = [round(abs(score - average), 1) for score in scores]
   return tuple(deviations)
   ```
4. **Опечатка в имени функции (`mean_absdeviation`)**  
   Исправление: Использовать единое имя `mean_abs_deviation`.

### 5. Оценка
- **Функциональность (0/50)**: Основная логика отсутствует.
- **Качество кода (10/30)**: Есть попытка обработки ошибок, но код нерабочий.
- **Стиль и тесты (5/20)**: Примеры вызовов есть, но не оформлены как тесты.  
**Снижено за**: отсутствие функции (-40), синтаксические ошибки (-20), нерабочие вызовы (-15), частичная реализация требований (-10).

### 6. Полное решение
```python
def mean_abs_deviation(*scores):
    if len(scores) == 0:
        raise ValueError("Нужно передать хотя бы один балл")
    average = sum(scores) / len(scores)
    deviations = [round(abs(score - average), 1) for score in scores]
    return tuple(deviations)

# Тесты
print(mean_abs_deviation(3, 5, 7, 9))  # (3.0, 1.0, 1.0, 3.0)

existing_scores = [10, 8, 6]
print(mean_abs_deviation(*existing_scores))  # (2.0, 0.0, 2.0)
```

---

Анализ выполнен моделью: GPT-4o
