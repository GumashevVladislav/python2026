# Домашнее задание. Урок 31. Словари и множества. паттерны использования, типичные ошибки

## Студент: Vladislav

На уроке у тебя есть попытка в задаче на мутацию словаря с низким баллом, поэтому в ДЗ будет упор на аккуратное суммирование и удаление.

## Задача 1. Сумма по заказам

Пример входных данных:
```python
orders = [
    ("Кирилл", 2),
    ("Оля", 1),
    ("Кирилл", 3),
]
```

### Требуется
1. Собрать словарь `tickets`, где ключ. имя, значение. суммарное количество билетов.
2. Вывести результат.

---

## Задача 2. Безопасное удаление клиентов

Пример входных данных:
```python
clients = [
    ("alpha", 5),
    ("beta", 3),
    ("alpha", 2),
]
blocked = ["beta", "gamma"]
```

### Требуется
1. Собрать словарь `orders_by_client`.
2. Для каждого имени из `blocked` удалить запись через `pop(name, 0)`.
3. Вывести итоговый словарь.

---

## Задача 3. Множества тэгов

Пример входных данных:
```python
tags_before = {"python", "sql", "git"}
tags_after = {"python", "docker"}
```

### Требуется
1. Найти тэги, которые остались общими.
2. Найти новые тэги, которые появились только позже.
3. Вывести результат.

---

## Задача 4. Подготовка к comprehensions

Пример входных данных:
```python
ratings = [
    ("lesson1", 5),
    ("lesson2", 4),
    ("lesson1", 3),
]
limit = 7
```

### Требуется
1. Собрать словарь `total_rating`.
2. Сформировать новый словарь только для уроков, где сумма не меньше `limit`.
3. Вывести оба словаря.
