# Задание 31.2. Словари и множества. паттерны использования, типичные ошибки
## Блок урока
Мутация словарей и обработка ошибок — Разбираем, как накапливать значения, безопасно удалять записи и не ловить KeyError при отсутствии данных.

## Условие задачи
Мейкер-спейс собирает статистику использования инструментов. Суммируй время использования и удали инструмент, который отправили на ремонт. Используй словарь tool_usage и аккуратные методы мутации.

## Пример входных данных
```python
usage_log = [('laser', 4), ('printer', 6), ('laser', 3), ('solder', 5)]
finished = ['printer']
```

## Пример выходных данных
```text
... зависит от реализации
```

## Критерии успешного выполнения
- Повторяющиеся ключи накапливаются в словаре tool_usage с помощью get или setdefault.
- Удаление записей выполняется через pop с запасным значением 'ещё не использовали'.
- После удаления выводится обновлённый словарь.
- Код работает даже если в списке finished нет части ключей.

## Подсказки
- Создай пустой словарь tool_usage и заполняй его в цикле по usage_log.
- Для удаления используй pop(key, запасное значение), чтобы избежать KeyError.
- Не забудь вывести значение, которое вернул pop.

## Частые ошибки и ограничения
- Не храни данные в списке, если уже построен словарь — это усложнит доступ.
- Не удаляй элементы через del без проверки.
- Не забывай выводить итог даже если удаления не было.
